---
"dalux-build-api": patch
---

Align inspection-plan and test-plan models, list responses, and pagination helpers with Python, and add a browser-safe Next.js proxy client that keeps Dalux credentials server-side.
