{
  "name": "dalux-build",
  "private": true,
  "description": "Monorepo for the Dalux Build API clients (Node.js, Python) and webhook server. See javascript/, python/, and webhook-server/.",
  "workspaces": [
    "javascript"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync-python-version.mjs",
    "release": "npm test --workspace=javascript && node scripts/publish-if-needed.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bruadam/dalux-build.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^3.0.0"
  }
}
